## Task0321
```
I. 异步相关编程：
1. 编写Promise.all 
2. 编写Promise并发控制

II. 简单编程：
1. 快速排序算法
2. 二分叉查找算法

III. 链表编程：
1. 删除倒数第n个链表节点
2. 合并两个有序链表
3. 判断链表是否有环

IV. Dom编程：
1. const ul = el('ul', {id: 'list'}, [
  el('li', {class: 'item'}, ['Item 1']),
  el('li', {class: 'item'}, ['Item 2']),
  el('li', {class: 'item'}, ['Item 3'])
]);
const ulRoot = ul.render();
document.body.appendChild(ulRoot);
实现el
```
