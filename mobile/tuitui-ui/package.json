{
  "name": "tuitui-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "browser-sync start --server --files \"**/**\"",
    "build:postcss": "npx postcss ./src/tuitui-ui.css -o ./dist/tuitui-ui.css -u postcss-import autoprefixer --no-map",
    "build:min": "npx postcss ./src/tuitui-ui.css -o ./dist/tuitui-ui.min.css -u postcss-import autoprefixer cssnano --no-map"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "browser-sync": "^2.26.7",
    "cssnano": "^4.1.10",
    "postcss-cli": "^6.1.3",
    "postcss-import": "^12.0.1"
  },
  "browserslist": [
    "last 2 versions"
  ],
  "dependencies": {
    "font-awesome": "^4.7.0"
  }
}
