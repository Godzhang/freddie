import Vue from "vue";
import Icon from "./Icon.vue";

Vue.component("Icon", Icon);

// const requireAll = (requireContext: any) =>
//   requireContext.keys().map(requireContext);
// const req = (require as RequireFunction).context("./svg", false, /\.svg$/);
// requireAll(req);
