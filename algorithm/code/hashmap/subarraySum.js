// export default (nums, k) => {
//   let result = 0;
//   for (let i = 0, len = nums.length; i < len; i++) {
//     let arr = nums.slice(i);
//     arr.reduce((sum, cur) => {
//       sum += cur;
//       if (sum === k) {
//         result++;
//       }
//       return sum;
//     }, 0);
//   }
//   return result;
// };

export default (nums, k) => {};
