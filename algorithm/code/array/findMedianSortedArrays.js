// export default (nums1, nums2) => {
//   Array.prototype.push.apply(nums1, nums2);
//   nums1.sort((a, b) => a - b);
//   let len = nums1.length;
//   let remain = len % 2;
//   let mIndex = len / 2;
//   if (remain === 1) {
//     return nums1[Math.floor(mIndex)];
//   } else {
//     return (nums1[mIndex] + nums1[mIndex - 1]) / 2;
//   }
// };

export default (nums1, nums2) => {};
